% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmarking_curves.R
\name{plot_order_EM_overlay2D}
\alias{plot_order_EM_overlay2D}
\title{Plot a 2D embedding colored by an ordering vector with EM means overlaid}
\usage{
plot_order_EM_overlay2D(
  coords,
  order_vec,
  mu_list,
  method = c("auto", "quantiles", "seq"),
  probs = seq(0, 1, by = 0.1),
  nbins = 30,
  include_lowest = TRUE,
  right = TRUE,
  only = NULL,
  fixed_limits = TRUE,
  pal = NULL,
  pch = 16,
  cex = 0.7,
  add_centroid = FALSE,
  centroid_pch = 4,
  centroid_cex = 1.2,
  centroid_lwd = 2,
  centroid_labels = FALSE,
  label_cex = 0.8,
  label_pos = 3,
  mu_order = NULL,
  mu_pch = 8,
  mu_col = "orange",
  mu_cex = 1,
  arrow_col = "orange",
  arrow_lwd = 2.5,
  arrow_len = 0.08,
  add_legend = FALSE,
  legend_loc = "none",
  legend_cex = 0.7,
  legend_title = NULL,
  asp = 1,
  xlab = "dim1",
  ylab = "dim2",
  main = "Ordering + EM means",
  ...
)
}
\arguments{
\item{coords}{Numeric matrix of dimension \eqn{n \times 2}.}

\item{order_vec}{Numeric vector of length \eqn{n}. Any continuous ordering/score vector.}

\item{mu_list}{A length-\eqn{K} list of mean vectors; each element must have at least 2 entries.
The first two entries are used as coordinates in the same 2D space as \code{coords}.}

\item{method}{Binning method for \code{order_vec}: \code{"quantiles"} or \code{"seq"}.
If \code{"auto"}, defaults to \code{"seq"}.}

\item{probs}{Quantile breakpoints (used when \code{method = "quantiles"}).}

\item{nbins}{Number of equal-width bins (used when \code{method = "seq"}).}

\item{include_lowest, right}{Passed to \code{\link[base]{cut}}.}

\item{only}{Optional subset of bins to plot; either integer bin indices or bin labels.}

\item{fixed_limits}{Logical; if \code{TRUE}, keep \code{xlim}/\code{ylim} fixed to the full
\code{coords} range even when \code{only} is used.}

\item{pal}{Optional vector of colors (length = number of plotted bins). If \code{NULL}, a blue-white-red palette is used.}

\item{pch, cex}{Point style for observations.}

\item{add_centroid}{Logical; if \code{TRUE}, add per-bin centroids (computed on plotted subset).}

\item{centroid_pch, centroid_cex, centroid_lwd, centroid_labels, label_cex, label_pos}{Centroid styling.}

\item{mu_order}{Optional permutation of \code{1:K} controlling the overlay order of \code{mu_list}.}

\item{mu_pch, mu_col, mu_cex}{Styling for mean points.}

\item{arrow_col, arrow_lwd, arrow_len}{Styling for arrows.}

\item{add_legend, legend_loc, legend_cex, legend_title}{Legend controls. Set \code{legend_loc = "none"} (or \code{NULL}) to suppress legend.}

\item{asp, xlab, ylab, main}{Plot controls passed to \code{\link[graphics]{plot}}.}

\item{...}{Additional arguments passed to \code{\link[graphics]{plot}}.}
}
\value{
Invisibly returns a list with kept indices, bins, colors, optional bin centroids,
and the plotted mean coordinates.
}
\description{
Given an \eqn{n \times 2} embedding (\code{coords}) and a continuous ordering vector
(\code{order_vec}), this function bins observations by \code{order_vec} (either by
quantiles or equal-width bins) and colors points by the resulting bins. It then overlays
SmoothEM component means (\code{mu_list}) as points connected by arrows.
}
