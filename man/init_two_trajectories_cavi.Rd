% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_partition_cavi.R
\name{init_two_trajectories_cavi}
\alias{init_two_trajectories_cavi}
\title{Initialize two CAVI trajectories for dual-ordering partitioning}
\usage{
init_two_trajectories_cavi(
  X,
  S = NULL,
  method = c("score", "mincor", "random_split"),
  method1 = c("PCA", "fiedler", "pcurve", "tSNE", "random", "isomap"),
  K = NULL,
  rw_q = 2L,
  ridge = 0,
  lambda_sd_prior_rate = NULL,
  lambda_min = 1e-10,
  lambda_max = 1e+10,
  sigma_min = 1e-10,
  sigma_max = 1e+10,
  discretization = c("quantile", "equal", "kmeans"),
  num_iter = 5L,
  seed = 42L,
  verbose = FALSE
)
}
\arguments{
\item{X}{Numeric matrix (\code{n x d}).}

\item{method}{Initialisation strategy for the second trajectory.}

\item{method1}{Initialisation method for the first trajectory.}

\item{K}{Number of pseudotime bins.}

\item{rw_q}{Random-walk order.}

\item{ridge}{Optional nugget added to the RW precision. Mainly useful for
internal diagnostics that compare intrinsic and properized priors.}

\item{lambda_sd_prior_rate}{Optional positive rate for an exponential prior
on \code{1 / sqrt(lambda_j^{(m)})}. The default \code{NULL} means no lambda
prior penalty. For backward compatibility, an explicit \code{0} is treated
the same way; it is only an alias for "no penalty" and does not
correspond to a literal exponential prior with rate zero.}

\item{lambda_min, lambda_max}{Bounds used when clipping the initial
feature-specific \code{lambda_j} values for each warm-start fit.}

\item{sigma_min, sigma_max}{Bounds for the feature-specific noise variance
\code{sigma2_j}. Forwarded to internal \code{cavi()} calls.}

\item{discretization}{Initial discretization scheme used when converting
ordering scores into \code{K} bins. For dual-ordering initialisation the
requested \code{K} is enforced strictly; if quantile cuts collapse, the
code falls back to equal-width bins so both orderings remain comparable.}

\item{num_iter}{Warm-start CAVI sweeps per fit.}

\item{seed}{Random seed used by \code{"random_split"}.}

\item{verbose}{Logical.}
}
\value{
A list with \code{fit1}, \code{fit2}, and \code{seed2}.
}
\description{
Initialize two CAVI trajectories for dual-ordering partitioning
}
