# Penalized ELBO for csmooth_em (optional)

Penalized ELBO for csmooth_em (optional)

## Usage

``` r
compute_penalized_ELBO_csmooth(
  X,
  Gamma,
  params,
  Q_K,
  lambda_vec,
  modelName,
  relative_lambda,
  eigen_tol = NULL,
  rw_q = 0L
)
```
