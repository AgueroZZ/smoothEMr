% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_SoftSmoothEM.R
\name{simulate_dual_trajectory}
\alias{simulate_dual_trajectory}
\title{Simulate a dual-trajectory dataset with configurable trajectory families}
\usage{
simulate_dual_trajectory(
  n = 200,
  d1 = 30,
  d2 = 30,
  d_noise = 20,
  sigma = 0.3,
  crossing = FALSE,
  seed = 42L,
  trajectory_family = c("sinusoidal", "sinusoidal"),
  signal_range = c(0.5, 2),
  linear_slope_range = c(0.8, 2),
  monotone_power_range = c(0.7, 2.5),
  quadratic_curvature_range = c(0.8, 2),
  quadratic_center_range = c(0.25, 0.75),
  intercept_sd = 0,
  sinusoid_freq = 1:4
)
}
\arguments{
\item{n}{Integer number of samples.}

\item{d1, d2}{Integer numbers of signal features assigned to trajectories A
and B.}

\item{d_noise}{Integer number of pure-noise features.}

\item{sigma}{Observation noise standard deviation added independently to each
simulated feature trajectory.}

\item{crossing}{Logical; if \code{TRUE}, the second latent ordering is
generated as a noisy reversed version of the first, making the two
orderings harder to disentangle.}

\item{seed}{Optional integer seed.}

\item{trajectory_family}{Character scalar or length-\code{M} vector
specifying the feature family for each ordering block. Each entry must be
one of \code{"sinusoidal"}, \code{"linear"}, \code{"monotone"}, or
\code{"quadratic"}.}

\item{signal_range}{Length-2 positive range controlling the per-feature
signal amplitude.}

\item{linear_slope_range}{Length-2 positive range for linear slopes.}

\item{monotone_power_range}{Length-2 positive range controlling the power-like
basis shapes used in the monotone family. The monotone generator mixes
several increasing bases internally to create more varied monotone curves.}

\item{quadratic_curvature_range}{Length-2 positive range for quadratic
curvature magnitudes.}

\item{quadratic_center_range}{Length-2 range inside \code{[0, 1]} for the
quadratic vertex.}

\item{intercept_sd}{Standard deviation of feature-specific intercept shifts.
Defaults to \code{0} to preserve the historical simulation baseline.}

\item{sinusoid_freq}{Integer vector of admissible sinusoid frequencies.}
}
\value{
A list with the same fields as
  \code{\link{simulate_intrinsic_trajectories}}, plus backward-compatible
  aliases \code{t1} and \code{t2}.
}
\description{
Backward-compatible convenience wrapper around
\code{\link{simulate_intrinsic_trajectories}} for the two-ordering case.
Preserves the old \code{t1}/\code{t2} return values and the
\code{crossing} shortcut.
}
