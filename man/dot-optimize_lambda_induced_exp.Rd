% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_utils.R
\name{.optimize_lambda_induced_exp}
\alias{.optimize_lambda_induced_exp}
\title{Optimize lambda under the induced exponential prior on 1/sqrt(lambda)}
\usage{
.optimize_lambda_induced_exp(eq_quad, r_rank, rate, lambda_min, lambda_max)
}
\arguments{
\item{eq_quad}{Positive numeric vector of posterior second moments.}

\item{r_rank}{Positive scalar RW precision rank.}

\item{rate}{Positive scalar rate parameter \eqn{\rho}.}

\item{lambda_min, lambda_max}{Positive bounds with \code{lambda_min <= lambda_max}.}
}
\value{
Numeric vector of optimized \code{lambda}.
}
\description{
Solves the penalized one-dimensional problem
\deqn{
\max_{\lambda \in [\lambda_{\min}, \lambda_{\max}]}
\frac{r_Q - 3}{2}\log \lambda
- \frac{q}{2}\lambda
- \rho \lambda^{-1/2},
}
where \eqn{q} is the current posterior second moment
\eqn{m^\top Q m + \mathrm{tr}(Q S)}.
}
\details{
The optimization is carried out in \eqn{\eta = \log \lambda}, for which the
objective is strictly concave and the derivative is monotone decreasing.
}
\keyword{internal}
