# Refresh pi/mu to the MAP solution under fixed sigma2 and lambda

Refresh pi/mu to the MAP solution under fixed sigma2 and lambda

## Usage

``` r
.refresh_map_mu_csmooth(
  X,
  Gamma,
  params,
  Q_K,
  lambda_vec,
  modelName,
  relative_lambda
)
```
