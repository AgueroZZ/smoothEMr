# Penalized observed-data objective for csmooth_em (optional)

Penalized observed-data objective for csmooth_em (optional)

## Usage

``` r
compute_log_joint_observed_csmooth(
  X,
  params,
  Q_K,
  lambda_vec,
  modelName,
  relative_lambda,
  eigen_tol = NULL,
  rw_q = 0L
)
```
