% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_cSmoothEM.R
\name{plot.csmooth_em}
\alias{plot.csmooth_em}
\title{Plot a csmooth_em object}
\usage{
\method{plot}{csmooth_em}(
  x,
  data = NULL,
  plot_type = c("scatterplot", "elbo", "mu"),
  dims = c(1, 2),
  two_panel = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{csmooth_em} object.}

\item{data}{Numeric matrix (n x d). Required when \code{plot_type="scatterplot"} unless stored in \code{x$data}.}

\item{plot_type}{One of \code{"scatterplot"}, \code{"elbo"}, \code{"mu"}.}

\item{dims}{Integer vector of length 1 or 2. Used for \code{"scatterplot"} and \code{"mu"}.}

\item{two_panel}{Logical; if TRUE and \code{plot_type="elbo"}, draw ELBO and objective in two panels.}

\item{verbose}{Logical; not passed to graphics (prevents "not a graphical parameter" warnings).}

\item{...}{Passed to the underlying plotting functions.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Visualization for a \code{csmooth_em} object.
\itemize{
  \item \code{plot_type="scatterplot"}: calls \code{plot_EM_embedding()} (same behavior as \code{smooth_em}).
  \item \code{plot_type="elbo"}: plots ELBO and penalized observed-data objective traces.
  \item \code{plot_type="mu"}: plots only the component means (with arrows for 2D).
}
}
