% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_partition.R
\name{score_one_coord_csmooth}
\alias{score_one_coord_csmooth}
\title{Score one coordinate against fixed responsibilities (Gamma) for csmoothEM (internal)}
\usage{
score_one_coord_csmooth(
  X,
  j,
  Gamma,
  Q_K,
  rw_q = 2,
  score_mode = c("ml", "none"),
  relative_lambda = TRUE,
  lambda_min = 1e-10,
  lambda_max = 1e+10
)
}
\arguments{
\item{X}{Numeric matrix \code{(n x d)}.}

\item{j}{Integer coordinate index in \code{1:d}.}

\item{Gamma}{Numeric matrix \code{(n x K)} of responsibilities.}

\item{Q_K}{Numeric matrix \code{(K x K)} base RW precision (lambda=1).}

\item{rw_q}{Integer \eqn{\ge 0}. Rank deficiency along K.}

\item{score_mode}{One of \code{"ml"} or \code{"none"}.}

\item{relative_lambda}{Logical.}

\item{lambda_min, lambda_max}{Bounds for lambda optimization (used when \code{score_mode="ml"}).}
}
\value{
A list with:
\itemize{
  \item \code{score}: scalar score for coordinate \code{j}.
  \item \code{one}: list with \code{mu_vec} (length K) and \code{sigma2} (scalar).
}
}
\description{
Convenience wrapper used in greedy partitioning. Returns both the scalar score and a
1D fitted object (\code{one}) that can be appended to csmooth-style parameters.
}
\keyword{internal}
