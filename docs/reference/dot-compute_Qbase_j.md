# Internal: compute Q_base_j for coordinate j under relative_lambda

Internal: compute Q_base_j for coordinate j under relative_lambda

## Usage

``` r
.compute_Qbase_j(pi, sigma2, Q_K, relative_lambda, modelName, j)
```
