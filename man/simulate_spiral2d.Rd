% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmarking_curves.R
\name{simulate_spiral2d}
\alias{simulate_spiral2d}
\title{Simulate a 2D Archimedean spiral (helix-like) with noise}
\usage{
simulate_spiral2d(n = 500, turns = 3, noise = 0.05, r0 = 0.2, r1 = 1, seed = 1)
}
\arguments{
\item{n}{Integer \eqn{\ge 1}. Number of samples.}

\item{turns}{Positive numeric. Number of revolutions over \eqn{t \in [0,1]}.}

\item{noise}{Nonnegative numeric. Standard deviation of Gaussian noise added
to both coordinates.}

\item{r0, r1}{Nonnegative numerics with \code{r1 >= r0}. Start/end radius.}

\item{seed}{Optional integer. If not NULL, sets the RNG seed via
\code{set.seed()}.}
}
\value{
A list with components:
\itemize{
  \item \code{obs}: numeric matrix \code{(n x 2)} with column names \code{c("x1","x2")}.
  \item \code{t}: numeric vector of length \code{n}, the latent ordering in \eqn{[0,1]}.
}
}
\description{
Simulates a 2D Archimedean spiral curve parameterized by a latent ordering
variable \eqn{t \in [0,1]}. The radius increases linearly with \eqn{t} and the
angle makes \code{turns} revolutions. Independent Gaussian noise is added to
each coordinate.
}
\examples{
sim <- simulate_spiral2d(n = 200, turns = 4, noise = 0.05, seed = 1)
head(sim$obs)
head(sim$t)

}
