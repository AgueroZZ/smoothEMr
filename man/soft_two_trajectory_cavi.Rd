% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_partition_cavi.R
\name{soft_two_trajectory_cavi}
\alias{soft_two_trajectory_cavi}
\title{Soft dual-trajectory partition using explicit mean-field CAVI}
\usage{
soft_two_trajectory_cavi(
  X,
  S = NULL,
  fit1_init = NULL,
  fit2_init = NULL,
  init_method = c("score", "mincor", "random_split"),
  init_method1 = c("PCA", "fiedler", "pcurve", "tSNE", "random", "isomap"),
  init_seed = 42L,
  K = NULL,
  discretization = c("quantile", "equal", "kmeans"),
  T_start = 5,
  T_end = 1,
  n_outer = 25L,
  inner_iter = 1L,
  max_converge_iter = 100L,
  tol_outer = 1e-05,
  rw_q = 2L,
  ridge = 0,
  lambda_sd_prior_rate = NULL,
  lambda_min = 1e-10,
  lambda_max = 1e+10,
  sigma_min = 1e-10,
  sigma_max = 1e+10,
  position_prior = c("adaptive", "fixed"),
  position_prior_init = NULL,
  partition_prior = c("adaptive", "fixed"),
  partition_prior_init = NULL,
  assignment_prior = NULL,
  ordering_alpha = NULL,
  hard_assign_final = FALSE,
  freeze_unused_ordering = TRUE,
  freeze_unused_ordering_threshold = 0.5,
  freeze_feature = TRUE,
  freeze_feature_weight_threshold = 0.1,
  drop_unused_ordering = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{X}{Numeric matrix (\code{n x d}).}

\item{fit1_init, fit2_init}{Optional initial \code{cavi} fits. If NULL,
they are initialized via \code{init_two_trajectories_cavi()}.}

\item{init_method, init_method1}{Initialization methods.}

\item{init_seed}{Random seed for \code{"random_split"}.}

\item{K}{Number of pseudotime bins.}

\item{discretization}{Initial discretization scheme used when converting
ordering scores into \code{K} bins. Dual-ordering initialisation enforces a
common \code{K} across the two orderings.}

\item{T_start, T_end}{Annealing temperatures.}

\item{n_outer}{Number of annealing steps.}

\item{inner_iter}{Number of weighted CAVI sweeps per outer step.}

\item{max_converge_iter}{Maximum number of exact \code{T=1} iterations.}

\item{tol_outer}{Relative objective tolerance for the stricter \code{T=1}
convergence rule used after annealing.}

\item{rw_q}{Random-walk order.}

\item{ridge}{Optional nugget added to the RW precision for both orderings.}

\item{lambda_sd_prior_rate}{Optional positive rate for an exponential prior
on \code{1 / sqrt(lambda_j^{(m)})}. The default \code{NULL} means no lambda
prior penalty. For backward compatibility, an explicit \code{0} is treated
the same way; it is only an alias for "no penalty" and does not
correspond to a literal exponential prior with rate zero.}

\item{lambda_min, lambda_max}{Bounds passed through to the weighted
single-ordering \code{cavi} updates.}

\item{sigma_min, sigma_max}{Bounds for the feature-specific noise variance
\code{sigma2_j}. Forwarded to internal \code{cavi()} calls.}

\item{position_prior}{Either \code{"adaptive"} or \code{"fixed"} for the
within-ordering component prior \code{pi}.}

\item{position_prior_init}{Optional length-\code{K} initial/fixed vector for
the within-ordering component prior \code{pi}. When
\code{position_prior = "fixed"} and this is \code{NULL}, a uniform prior
over the \code{K} positions is used.}

\item{partition_prior}{Either \code{"adaptive"} or \code{"fixed"} for the
ordering-usage prior \code{omega}.}

\item{partition_prior_init}{Optional length-2 initial/fixed vector for the
ordering prior \code{omega}. Used only when
\code{partition_prior = "fixed"}.}

\item{assignment_prior}{Deprecated compatibility argument for the old
partition-prior API.}

\item{ordering_alpha}{Deprecated compatibility argument used only for the
legacy \code{assignment_prior = "dirichlet"} path.}

\item{hard_assign_final}{Logical.}

\item{freeze_unused_ordering}{Logical; if \code{TRUE}, unused orderings may
be frozen and skipped in subsequent updates.}

\item{freeze_unused_ordering_threshold}{Non-negative feature-mass threshold
used when \code{freeze_unused_ordering = TRUE}.}

\item{freeze_feature}{Logical; if \code{TRUE}, feature-ordering pairs with
sufficiently small posterior weight are frozen and their trajectory
contribution is neutralized.}

\item{freeze_feature_weight_threshold}{Non-negative threshold on
\code{w_{jm}} used when \code{freeze_feature = TRUE}.}

\item{drop_unused_ordering}{Logical; if \code{TRUE}, user-facing wrappers may
compact the returned partition view to the active orderings after fitting.
With \code{FALSE}, the exposed \code{$objective_history} keeps the
fixed-requested-\code{M} comparison semantics of
\code{\link{soft_partition_cavi}}; with \code{TRUE}, it becomes the
post-drop fitting objective of the active model.}

\item{verbose}{Logical.}
}
\value{
An object of class \code{"soft_partition_cavi"} with fields:
  \code{$fits} (list of 2 cavi fits), \code{$pi_weights} (d x 2),
  \code{$assign}, \code{$M}, \code{$objective_history}, etc. The
  \code{$objective_history} semantics match
  \code{\link{soft_partition_cavi}}.
}
\description{
Wrapper around \code{\link{soft_partition_cavi}} with \code{M = 2}. Provides
the original dual-trajectory interface for backward compatibility.
}
