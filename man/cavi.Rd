% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_cavi.R
\name{cavi}
\alias{cavi}
\title{Fit the recommended CAVI model for a single MPCurver ordering}
\usage{
cavi(
  X,
  K = NULL,
  method = c("PCA", "fiedler", "pcurve", "tSNE", "random", "isomap"),
  responsibilities_init = NULL,
  pi_init = NULL,
  sigma2_init = NULL,
  lambda_init = NULL,
  rw_q = 2L,
  ridge = 0,
  discretization = c("quantile", "equal", "kmeans"),
  fix_lambda = FALSE,
  lambda_sd_prior_rate = NULL,
  lambda_min = 1e-10,
  lambda_max = 1e+10,
  sigma_min = 1e-10,
  sigma_max = 1e+10,
  max_iter = 100L,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{X}{Numeric matrix (\eqn{n \times d}).}

\item{K}{Number of components. If NULL, defaults to \code{max(2, min(50, floor(n/5)))}.}

\item{method}{Initialization ordering method when \code{responsibilities_init} is NULL.}

\item{responsibilities_init}{Optional initial responsibility matrix (\eqn{n \times K}).}

\item{pi_init}{Optional initial mixture proportions.}

\item{sigma2_init}{Optional initial feature-specific variances.}

\item{lambda_init}{Optional initial feature-specific smoothness vector.}

\item{rw_q}{Random-walk order for the prior precision along components.}

\item{ridge}{Ridge added to \code{Q_K}.}

\item{discretization}{Discretization method for ordering-based initialization.}

\item{fix_lambda}{Logical. If \code{TRUE}, skip variational lambda updates
and keep \code{lambda_j} fixed at the initial values throughout fitting.}

\item{lambda_sd_prior_rate}{Optional positive rate for an exponential prior
on \code{1 / sqrt(lambda_j)}. The default \code{NULL} means no lambda prior
penalty. For backward compatibility, an explicit \code{0} is treated the
same way; it is only an alias for "no penalty" and does not correspond to
a literal exponential prior with rate zero.}

\item{lambda_min, lambda_max}{Bounds for \code{lambda_j}.}

\item{sigma_min, sigma_max}{Bounds for \code{sigma_j^2}.}

\item{max_iter}{Maximum number of CAVI sweeps. \code{0} returns the
initialization-only state.}

\item{tol}{Relative ELBO tolerance.}

\item{verbose}{Logical.}
}
\value{
An object of class \code{"cavi"} with components including:
\itemize{
  \item \code{$params}: current \code{pi}, \code{mu}, and \code{sigma2}
  \item \code{$posterior}: Gaussian posterior summaries for feature trajectories
  \item \code{$gamma}: cell-by-component responsibilities
  \item \code{$lambda_vec}: feature-specific smoothness parameters
  \item \code{$elbo_trace}: the monotone variational objective used for diagnostics
  \item \code{$loglik_trace}: a plug-in observed log-likelihood diagnostic
}
}
\description{
Recommended variational pipeline for a single-ordering MPCurver model with
feature-specific noise levels \eqn{\sigma_j^2} and smoothness parameters
\eqn{\lambda_j}. The latent trajectory for feature \eqn{j} is a length-\eqn{K}
vector with a GMRF prior along the component axis.

This function is now the main single-ordering MPCurver backend. In contrast
to the legacy \code{csmooth_em} path, it keeps an explicit Gaussian
posterior over feature trajectories, with posterior means \eqn{m_j} and
covariances \eqn{S_j}.
}
