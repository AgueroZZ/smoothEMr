% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_initialization.R
\name{parallel_initial}
\alias{parallel_initial}
\title{Run multiple SmoothEM initializations in parallel}
\usage{
parallel_initial(
  X,
  methods = c("PCA", "tSNE", "random", "fiedler", "multi_scale"),
  num_iter = 1,
  num_cores = 2,
  m_max = 6,
  K = NULL,
  seed = NULL,
  adaptive = TRUE,
  lambda_min = 1e-08,
  lambda_max = 1e+08,
  quiet = TRUE,
  ...
)
}
\arguments{
\item{X}{Numeric matrix (n x d).}

\item{methods}{Character vector of methods to try. Defaults to
\code{c("PCA","tSNE","random","fiedler","multi_scale")}.}

\item{num_iter}{Integer >= 1. Total number of SmoothEM outer iterations to run for each method.}

\item{num_cores}{Integer >= 1. Number of cores for parallel execution.}

\item{m_max}{Integer >= 1. Used by \code{multi_scale} and to set default \code{K}.}

\item{K}{Optional integer grid size for non-multi-scale methods. If NULL, uses \code{2^m_max+1}.}

\item{seed}{Optional base seed for reproducibility. If provided, each method gets a deterministic
derived seed.}

\item{adaptive}{Logical; if TRUE, enables adaptive lambda behavior in \code{initialize_smoothEM()}.}

\item{lambda_min, lambda_max}{Positive bounds used to clip lambda when \code{adaptive=TRUE}.}

\item{quiet}{Logical; suppress messages from workers.}

\item{...}{Extra args passed to \code{initialize_smoothEM()} (e.g. \code{rw_q}, \code{lambda},
\code{relative_lambda}, \code{modelName}, etc.).}
}
\value{
A named list of \code{smooth_em} objects (or \code{NULL} for failed fits),
with attributes:
\itemize{
  \item \code{summary}: a data.frame summarizing last ELBO / last objective / last lambda for each method.
}
}
\description{
Runs \code{initialize_smoothEM()} for a set of initialization methods in parallel.
Each method is fit for \code{num_iter} total SmoothEM iterations. Internally,
\code{initialize_smoothEM()} warm-starts with exactly one EM iteration, and if
\code{num_iter > 1}, continues via \code{do_smoothEM()} (where adaptive lambda updates
may be enabled).

For compatibility with \code{method="multi_scale"}, if \code{K} is not provided,
then non-multi-scale methods default to \code{K = 2^m_max + 1}.
}
