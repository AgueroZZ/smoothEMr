% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_cavi.R
\name{do_cavi}
\alias{do_cavi}
\title{Continue CAVI sweeps on an existing \code{cavi} fit}
\usage{
do_cavi(
  object,
  iter = 1L,
  tol = NULL,
  lambda = NULL,
  lambda_sd_prior_rate = NULL,
  lambda_min = NULL,
  lambda_max = NULL,
  sigma_min = NULL,
  sigma_max = NULL,
  verbose = FALSE
)
}
\arguments{
\item{object}{A \code{cavi} object.}

\item{iter}{Integer maximum number of additional CAVI sweeps.}

\item{tol}{Optional convergence tolerance. If \code{NULL}, reuse the
original fit's tolerance.}

\item{lambda}{Optional scalar or d-vector. If provided, overrides
\code{lambda_vec} in the fit and fixes lambda (skips variational
lambda updates).}

\item{lambda_sd_prior_rate}{Optional positive rate for the induced
exponential prior on \code{1 / sqrt(lambda_j)}. If \code{NULL}, reuse the
value stored in the original fit's \code{$control}. An explicit \code{0}
is treated as "no penalty" for backward compatibility and does not
represent a literal exponential prior with rate zero.}

\item{lambda_min, lambda_max}{Optional bounds for \code{lambda_j}. If
\code{NULL}, reuse the values stored in the original fit's
\code{$control}. Ignored when \code{lambda} is provided.}

\item{sigma_min, sigma_max}{Optional bounds for \code{sigma_j^2}. If
\code{NULL}, reuse the values stored in the original fit's
\code{$control}.}

\item{verbose}{Logical.}
}
\value{
An updated \code{cavi} object with extended traces. The returned
  object keeps the full accumulated \code{$elbo_trace}, \code{$loglik_trace},
  \code{$lambda_trace}, \code{$sigma2_trace}, and \code{$pi_trace}.
}
\description{
Continue CAVI sweeps on an existing \code{cavi} fit
}
