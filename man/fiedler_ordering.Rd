% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_initialization.R
\name{fiedler_ordering}
\alias{fiedler_ordering}
\title{Fiedler ordering from a kNN graph}
\usage{
fiedler_ordering(
  X,
  k = 15,
  weight = c("binary", "rbf", "inv"),
  sigma = NULL,
  keep = c("giant", "all"),
  return_full = TRUE
)
}
\arguments{
\item{X}{Numeric matrix \eqn{n \times D} (rows are observations).}

\item{k}{Number of nearest neighbors.}

\item{weight}{Similarity type: \code{"rbf"}, \code{"inv"}, or \code{"binary"}.}

\item{sigma}{Bandwidth for \code{weight="rbf"}; if \code{NULL}, uses median kNN distance.}

\item{keep}{Component handling: \code{"giant"} keeps the largest connected component;
\code{"all"} uses all nodes (may be unstable if disconnected).}

\item{return_full}{If \code{TRUE} (default), return \code{t} of length \eqn{n}
with \code{NA} for nodes excluded by \code{keep="giant"}.
If \code{FALSE}, return \code{t} only for the kept nodes.}
}
\value{
A list with \code{t}, \code{keep_idx}, and \code{n_components}.
}
\description{
Builds a kNN graph on rows of \code{X}, computes a 1D ordering coordinate using
the Fiedler direction of a normalized graph operator, and returns an ordering
\code{t} scaled to \eqn{[0,1]}.
}
