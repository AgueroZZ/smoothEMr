# Summary method for smooth_em objects

Summary method for smooth_em objects

## Usage

``` r
# S3 method for class 'smooth_em'
summary(object, ...)
```

## Arguments

- object:

  A `smooth_em` object.

- ...:

  Passed through (unused).

## Value

An object of class `summary.smooth_em`.
